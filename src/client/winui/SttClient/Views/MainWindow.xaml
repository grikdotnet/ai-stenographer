<Window
    x:Class="SttClient.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Speech-to-Text">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid x:Name="AppTitleBar"
              Grid.Row="0"
              Height="48"
              Padding="16,0,144,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE720;" FontSize="20"
                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                    <TextBlock Text="Speech-to-Text" FontWeight="SemiBold" FontSize="13"/>
                    <TextBlock Text="Real-time Recognition" FontSize="10" Opacity="0.6"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <Ellipse x:Name="StatusDot" Width="8" Height="8"
                         Fill="{ThemeResource SystemFillColorAttentionBrush}"/>
                <TextBlock x:Name="StatusLabel"
                           Text="Idle"
                           FontSize="11" Opacity="0.7" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Padding="16,4,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="4">
                <Button x:Name="PauseButton"
                        Click="PauseButton_Click"
                        IsEnabled="{x:Bind ViewModel.IsRunningOrPaused, Mode=OneWay}"
                        Style="{ThemeResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon x:Name="PauseButtonIcon" Glyph="&#xE768;" FontSize="12"/>
                        <TextBlock x:Name="PauseButtonLabel" Text="Start"/>
                    </StackPanel>
                </Button>

                <ToggleButton x:Name="InsertionToggle"
                              Click="InsertionToggle_Click">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE8C1;" FontSize="12"/>
                        <TextBlock x:Name="InsertionLabel" Text="Insert: OFF"/>
                    </StackPanel>
                </ToggleButton>

                <Button x:Name="ClearButton"
                        Click="ClearButton_Click">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE74D;" FontSize="12"
                                  Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                        <TextBlock Text="Clear"
                                   Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <TextBlock Grid.Column="1"
                       Text="Ctrl+Space to insert at cursor"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="11"
                       Opacity="0.45"/>

            <Button Grid.Column="2"
                    x:Name="SettingsButton"
                    Click="SettingsButton_Click">
                <FontIcon Glyph="&#xE713;" FontSize="16"/>
            </Button>
        </Grid>

        <!-- Transcript Card -->
        <Grid Grid.Row="2" Padding="16,0,16,16">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Transcription"
                               FontWeight="SemiBold"
                               FontSize="12"
                               Opacity="0.7"
                               Margin="0,0,0,8"/>

                    <StackPanel Grid.Row="1"
                                x:Name="EmptyStatePlaceholder"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Spacing="12">
                        <FontIcon Glyph="&#xE720;" FontSize="36" Opacity="0.2"
                                  HorizontalAlignment="Center"/>
                        <TextBlock Text="Start speaking to see transcription here"
                                   Opacity="0.4"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1"
                                  x:Name="TranscriptScrollViewer"
                                  VerticalScrollBarVisibility="Auto"
                                  Visibility="Collapsed">
                        <StackPanel Spacing="4">
                            <TextBlock x:Name="FinalizedTextBlock"
                                       Text="{x:Bind ViewModel.FinalizedText, Mode=OneWay}"
                                       TextWrapping="Wrap"
                                       IsTextSelectionEnabled="True"/>
                            <TextBlock x:Name="PartialTextBlock"
                                       Text="{x:Bind ViewModel.PartialText, Mode=OneWay}"
                                       TextWrapping="Wrap"
                                       Opacity="0.5"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
