<Window
    x:Class="SttModelDownloader.Views.DownloadWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Speech-to-Text — Required Models">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="The following model must be downloaded:"
                   FontSize="14"
                   TextWrapping="Wrap"/>

        <StackPanel Grid.Row="1" Spacing="4">
            <TextBlock Text="Parakeet STT Model (~1.2 GB)"
                       FontWeight="SemiBold"/>
        </StackPanel>

        <ProgressBar x:Name="DownloadProgressBar"
                     Grid.Row="2"
                     Maximum="100"
                     Minimum="0"
                     Visibility="Collapsed"/>

        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="3"
                   Text=""
                   FontSize="12"
                   Opacity="0.7"
                   TextWrapping="Wrap"/>

        <TextBlock x:Name="ErrorTextBlock"
                   Grid.Row="4"
                   Text=""
                   FontSize="12"
                   Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"/>

        <StackPanel Grid.Row="5" Spacing="4" Margin="0,8,0,0">
            <TextBlock Text="Parakeet-TDT-0.6B-v3 by NVIDIA (CC BY 4.0)"
                       FontSize="11"
                       Opacity="0.6"/>
            <TextBlock Text="Silero VAD by Silero Team (MIT)"
                       FontSize="11"
                       Opacity="0.6"/>
        </StackPanel>

        <StackPanel Grid.Row="7"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8">
            <Button x:Name="DownloadButton"
                    Click="OnDownloadClicked"
                    Style="{ThemeResource AccentButtonStyle}">
                Download
            </Button>
            <Button x:Name="ExitButton"
                    Click="OnExitClicked">
                Exit
            </Button>
        </StackPanel>
    </Grid>
</Window>
